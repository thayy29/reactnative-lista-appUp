import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#000",
  },

  title: {
    color: "white",
    paddingTop: 10,
    fontSize: 20,
  },

  secondaryText: {
    color: "gray",
    paddingTop: 1,
    fontSize: 20,
  },
});
